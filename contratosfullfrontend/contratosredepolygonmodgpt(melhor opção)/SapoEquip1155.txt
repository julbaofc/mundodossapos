// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "./SapoCore.sol";

contract SapoEquip1155 is SapoCore {
    address public frogs;
    address public registry;

    constructor(address _owner) SapoCore(_owner) {}
    function setLinks(address _frogs, address _registry) external onlyOwner {
        frogs = _frogs;
        registry = _registry;
    }
}
